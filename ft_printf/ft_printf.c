#include "libft.h"
#include "ft_printf.h"

int     ft_printf(const char *format, ...)
{
    va_list ap;
    va_start(ap, format);
    va_end(ap);
    return (1);
}

/*

    this is a %s to be formated, %s

*/